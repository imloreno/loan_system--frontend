type Theme = {
  background: string;
  primary: string;
  secondary: string;
  shadow: string[];
  buttons: {
    primary: string;
    error: string;
    success: string;
    warning: string;
    info: string;
  };
};

export default Theme;
